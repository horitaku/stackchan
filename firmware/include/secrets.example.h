#pragma once

// Copy this file to secrets.h in the same directory and set local values.
// Never commit real credentials.

#define FW_WIFI_SSID "YOUR_WIFI_SSID"
#define FW_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
#define FW_WS_TOKEN "OPTIONAL_WS_TOKEN"
